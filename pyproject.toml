[project]
name = "csv-to-json-converter"
version = "0.1.0"
description = "Convert CEFR vocabulary CSV files to JSON format"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
convert-csv-to-json = "convert_csv_to_json:main"

